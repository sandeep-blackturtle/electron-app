import React from 'react'

const Model = (props) => {

    return (
        <div cloassName="model">
            <div className="model-container">
                { props.children }
            </div>
        </div>
    );
}
